<div class="container mt-4">
	<%= simple_form_for [@service, @offer] do |f| %>
	  <%= f.input :title, label: 'Titre' %>
	  <%= f.association :service, input_html: { class: "beautiful-dropdown" }, include_hidden: false %>
	  <%= f.input :description %>
	  <%= f.input :price, label: 'Prix au m2 (en €)' %>
	  <%= f.button :submit, 'Ajouter mon offre', class: 'btn btn-primary' %>
	<% end %>
</div>